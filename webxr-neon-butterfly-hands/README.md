# webxr-neon-butterfly-hands

Single-file HTML/CSS/JS visual demo ("Neon Butterfly Hands"). Self-contained,
no build step.

## How to run

Open `index.html` directly in a browser, or serve the directory:

```sh
python3 -m http.server -d . 8000
# open http://localhost:8000
```
