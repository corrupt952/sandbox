[project]
name = "coremltools-dynshape-poc"
version = "0.1.0"
description = "Probe: does coremltools trace/predict a zero-length dynamic-shape gather (the pattern that broke onnxruntime's CoreML EP on Style-Bert-VITS2)?"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "coremltools>=9.0",
    "torch>=2.13.0",
]
